name: Testing on stg 

on:
  pull_request:
    types:
      - "opened"
      - "synchronize"
    branches: 
      - "master"

env:
  MYENV: test 
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test with docker-compose stack
        run: |
          cd ${GITHUB_WORKSPACE}
          docker-compose up -d --remove-orphans db app 
          docker-compose exec -T -w /app app composer install 
          echo "----container ls -alt via wkdir=/app ------"
          docker-compose exec -T -w /app app ls -alt 
          echo "----------"
          echo "----container ls -alt via wkdir=/app/./tests ------"
          docker-compose exec -T -w /app app ls -alt ./tests 
          echo "----------"
          # docker-compose exec -T -w /app app vendor/bin/phpunit 
